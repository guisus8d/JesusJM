[build]
  publish = "dist"           # Carpeta donde Astro genera el build
  functions = "netlify/functions"  # Carpeta de tus funciones

[functions]
  node_bundler = "esbuild"   # Método para compilar

# Redirecciones opcionales
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200